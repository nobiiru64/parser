<?php

/*

if (!is_file(realpath($env.'/.env')))
    throw new EnvException("Файл .env не найден");

$envLoaded = Dotenv::create($env)->load();

if (!$envLoaded)
    throw new EnvException("Файл .env не загрузился");

$this->imagesPath = getenv('IMAGES_PATH');

if (!is_dir($this->imagesPath))
    throw new \Parser\InvalidPathException('Папка с изображениями не доступна');
*/

/*
        if (!is_writeable(realpath($this->symlink)))
            throw new \Parser\InvalidPathException("Конечная папка не доступна для записи {$this->symlink}");
        */

/*

        if (!is_file($this->xmlInput))
            throw new \Parser\InvalidFileException('');
 */
/*

serialize XML

     /*
                try {
                } catch (\Exception $e) {
                    // throw new \Exception($e);
                    delete_directory($this->envPath . $this->imagesOutput . "apartment{$this->datetime}");
                }
                */

